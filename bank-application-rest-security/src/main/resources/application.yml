server:
  port: 8081

#spring:
#  security:
#    user:
#      name: root
#      password: root
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/db_spring_security
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
